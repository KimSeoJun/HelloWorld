extends layout

block content
  h1= title
  - each student in students
    form(mathod="POST" action="/update")
      p
      input(type="text" name="no" value="#{board.no}")
      input(type="text" name="name" value="#{board.title}" size="5")
      input(type="text" name="gender" value="#{board.context}" size="5")
      input(type="submit" value="글목록")